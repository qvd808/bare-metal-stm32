# dev_board.cfg – STM32F446RE Nucleo, portable

# 1. ST-Link interface
source [find interface/stlink.cfg]

# 2. Don't force transport here; both builds will auto-select:
#    - vanilla: hla_swd
#    - esp32:   swd (dapdirect)
# You will see a DEPRECATED warning, but it's harmless.

# 3. Adapter speed
adapter speed 1800

# 4. STM32F4 target
source [find target/stm32f4x.cfg]

# Optional:
# reset_config srst_only
